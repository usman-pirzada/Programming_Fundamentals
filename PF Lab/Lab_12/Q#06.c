#include <stdio.h>
#include <stdlib.h>

int main() {
    printf("Enter the dimentions of first matrix (m X n): ");
    scanf("%d %d", &m, &n);
    printf("Enter the dimension of second matrix (%d X p)", n);
    scanf("%d", &p);

    int *arr1 = (int*) calloc();
    return 0;
}